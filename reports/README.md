# Reports

Generated experiment outputs are written here by the CLI and are ignored by
git. Detailed linkage and privacy-trial files may contain sensitive or
re-identification-adjacent information, so public examples should use synthetic
data or redacted outputs.

Recommended public demo:

```bash
python main.py run-demo --synthetic
```
